public class User
{
	int userId;
	int orgId;
	int numFiles;
	int numBytes;
	
	public User(int UserId, int OrgId, int NumFiles, int NumBytes){
		userId = UserId;
		orgId = OrgId;
		numFiles = NumFiles;
		numBytes = NumBytes;
	}
}